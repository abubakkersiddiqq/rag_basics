packages have been installed
 1. pip install langchain_openapi
 2. p install langchain-core
 3. pip install -U langchain-openai


install
pip install langchain langchain-community chromadb pypdf

after text pdf extraction and final moment

langchain-openai 
sentence-transformers

install
pip install langchain langchain-community chromadb pypdf

after text pdf extraction and final moment

langchain-openai 
sentence-transformers

Notebook → FastAPI, nothing exotic
PyPDFLoader
RecursiveCharacterTextSplitter  
OpenAIEmbeddings (or HuggingFaceEmbeddings if cost is a concern)
Chroma (persisted to disk, same folder mounted in Docker)
as_retriever(search_type="mmr", search_kwargs={"k": 4, "fetch_k": 20})
RetrievalQA.from_chain_type() or LCEL
FastAPI + uvicorn
Docker → Render

If you reach the free limit in LangChain, you'll receive a 429 Too Many Requests error.
At that point, 
you can either upgrade to a PRO account for higher limits or switch to local embeddings
 (running on your own CPU/GPU), which is always completely free. 

 ---
 for RetrievalQA 
 we chose stuffchain type beacuse of its nature 